import {getOffer} from './offer-generation.js';

/*const serverOffers = [];
const setData = (data) => {
  serverOffers.push(data);
};

fetch('https://25.javascript.pages.academy/keksobooking/data')
  .then((response) => response.json())
  .then((data) => {
    setData(data)
    //serverOffers = offers.slice(0, 10); нельзя так
  });

console.log(getOffer(serverOffers));*/
